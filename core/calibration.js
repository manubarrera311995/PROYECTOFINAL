/**
 * Calibración de moods y heurísticas emocionales.
 * Fase 2 — port desde audio-dna/index.html
 */

export function notImplemented(fn) {
  throw new Error(`${fn} aún no implementado (Fase 2)`);
}
