/**
 * Procesamiento de señal (FFT, espectrograma, features).
 * Fase 2 — port desde Web Worker de audio-dna/index.html
 */

export function notImplemented(fn) {
  throw new Error(`${fn} aún no implementado (Fase 2)`);
}
