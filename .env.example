# Rutas relativas al repo hermano audio-dna (misma carpeta padre)
CSV_DIR=../audio-dna
OUTPUT_BASE=../audio-dna

# Paralelismo (Fase 6)
DOWNLOAD_WORKERS=4
ANALYZE_WORKERS=3

# Comportamiento del pipeline
SKIP_EXISTING=true
DELETE_WAV_AFTER=false
