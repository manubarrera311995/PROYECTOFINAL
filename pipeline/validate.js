/**
 * Validación de JSON generados y reporte quality_{year}.json
 * Fase 6
 */

export async function validateEdition({ year, dataDir, reportDir }) {
  throw new Error(
    `validateEdition aún no implementado (Fase 6): ${year} en ${dataDir}`
  );
}
