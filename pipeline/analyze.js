/**
 * Decodificación WAV → PCM → core/analyzer → JSON.
 * Fase 4
 */

export async function analyzeWav({ wavPath, outputPath, metadata = {} }) {
  throw new Error(
    `analyzeWav aún no implementado (Fase 4): ${wavPath} → ${outputPath}`
  );
}
